name: 'modern_data_platform'
version: '1.0.0'
config-version: 2

profile: 'modern_data_platform'

model-paths: ['models']
macro-paths: ['macros']
target-path: 'target'
clean-targets:
  - 'target'
  - 'dbt_packages'

models:
  modern_data_platform:
    staging:
      +materialized: view
    curated:
      +materialized: table
    analytics:
      +materialized: table